# Rendered by systemd-service: restarts swarm service data_postgres after 5 failed healthchecks

[Unit]
Description=restart data_postgres after a failed healthcheck

[Service]
Type=oneshot
ExecStart=systemctl restart data_postgres.service
