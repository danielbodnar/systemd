# SPDX-License-Identifier: LGPL-2.1-or-later
#
# One read-only root partition holding the merged image tree, for a DDI that a
# service mounts with RootImage=. Used by make-ddi.sh with --copy-source=.
[Partition]
Type=root
Format=erofs
CopyFiles=/
Minimize=best
